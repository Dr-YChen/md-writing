---
name: md-writing
description: 生成或优化 Markdown 文档。当用户提到"写 Markdown"、"写文档"、"整理文档"、"润色文档"、"优化文档"、"改善文档结构"、"翻译并保存为 Markdown"、"输出为 .md 文件"、"帮我写一篇"、"润色这段文字"、"改善这篇文章"、"检查这个文档"、"把这个翻译成 md"、"帮我写个文档"、"输出 markdown"、"写成 md 格式"时，必须使用此技能。无论用户是否明确提到"skill"，只要涉及文档写作、文档优化、文档润色、文档翻译任务就应该使用此技能。
---

# MD Writing

用于生成新的 Markdown 文档，或优化/润色已有文档的内容、结构和表达。

## 核心原则

### 表达方式

**关于括号的使用**：

- **高频缩写**：如果一个缩写在正文中会反复出现，可以在第一次出现时用括号标注，例如：`OTFS（正交时频空调制）是一种……`
- **低频术语**：直接用，无需括号解释

不要为了"解释"而加括号。括号是用来帮助读者识别即将反复遇到的缩写，不是用来给所有术语加备注的。

**不要自创比喻**：基于事实描述，避免使用个人创造的比喻来解释概念。直接陈述事实，比使用比喻更能准确传达意思。

**逻辑清晰**：使用清晰的文章结构，让读者能顺畅地从头读到尾。每个段落应有明确的主题句。

### 文档结构

```
# 标题

## 第一节
内容...

## 第二节
内容...
```

- 使用多级标题（`#`、`##`、`###`）构建层次
- 每一节聚焦一个主题
- 段落之间用空行分隔
- 避免过长的段落（超过 200 字考虑拆分成多段）

### 内容组织

1. **开头**：简要说明本文要讲什么，读者能获得什么
2. **正文**：按逻辑顺序展开（从概述到细节，或从原因到结果）
3. **收尾**：总结要点，必要时提供延伸阅读或下一步行动

### 语言风格

- 使用主动语态
- 术语保持一致（同一概念全程用同一词汇）
- 名词和动词搭配协调
- 删除冗余的修饰词
- 数字的表述应符合上下文（阿拉伯数字 vs 中文数字）
- **翻译时一律中文化**：所有英文单词都应译为中文，不要直接留英文单词（如 resilience→韧性，legacy→遗留，reciprocal→互易的）。只有一种例外：第一次出现英文术语时在括号附上原文

### 写作检查清单

每完成一段后，自查以下问题：
- [ ] 括号是否必要？（只有高频缩写才加括号）
- [ ] 是否有直接留着的英文单词？（应全部中文化）
- [ ] 术语是否全文一致？（同一概念用同一词汇）
- [ ] 逻辑是否通顺？（读一遍看是否拗口）
- [ ] 数学公式是否用了正确的 `$$...$$`（行间）或 `$...$`（行内）？
- [ ] 图片语法是否在代码块外？
- [ ] 代码块是否仅用于展示代码本身？

### Markdown 格式规范（强制）

**数学公式**：
- 行间公式（单独占一行）必须使用 `$$...$$`
- 行内公式（嵌入文字中）必须使用 `$...$`
- 禁止将公式放入代码块中

**图片**：
- 图片使用标准 Markdown 语法：`![alt](图片路径)`
- 不要将图片语法放入代码块中
- alt 文字应简洁描述图片内容

**代码块**：
- 仅在需要展示代码本身时使用代码块（`````` ``` ```````）
- 不要用代码块包裹：图片语法、数学公式、非代码内容

**标题层级**：
- 一个文档只用一个 `#` 一级标题
- 使用 `##` 和 `###` 构建层次结构
- 避免超过三级标题

### 翻译强制词表

以下词汇在翻译任务中**必须译为中文**，不得直接留英文：

| 英文 | 中文 |
|------|------|
| resilience / resilient | 韧性 / 抗扰性 |
| reciprocal | 互易的 / 倒易的 |
| legacy | 遗留的 |
| sub- / super- | 子- / 超- |
| coherent / incoherent | 相干的 / 非相干的 |
| localized / non-localized | 局部化的 / 非局部化的 |
| invariant / variant | 不变的 / 变化的 |
| separable / orthogonality | 可分离的 / 正交性 |
| perturbation | 扰动 |
| achievable | 可达的 |
| nominal | 标称的 |
| extrinsic / intrinsic | 外在的 / 内在的 |

如果不确定某个词是否应该翻译，就翻译它。

### 处理已有文档

如果用户提供了一篇文档让你优化：
1. 先通读全文，理解核心信息和结构
2. 检查表达问题（括号滥用、逻辑跳跃、冗余内容）
3. 修正问题，保持原有信息完整
4. 提升可读性和结构清晰度

### 处理翻译任务

如果要求翻译并保存为 Markdown：
1. 准确传达原文意思，不漏译
2. **一律中文化**：所有英文单词都译成中文，第一次出现时在括号附上英文原文（不是加括号解释，而是附原文）
3. 保持原文的段落结构和逻辑顺序

## 输出格式

输出应为纯 Markdown 格式，包含：
- 适当的标题层级
- 清晰的分段
- 简洁但完整的内容

如果输出过长（超过 500 行），考虑拆分成多篇文档，并在开头提供索引。
